class User {
	constructor(userId = null) {
		this.userId = userId
		this.userInfo = null
	}
	//when logged on, will set the constructor to add this info

	getFullName() {
		// return `${this.firstname} ${this.lastName}
	}
}
